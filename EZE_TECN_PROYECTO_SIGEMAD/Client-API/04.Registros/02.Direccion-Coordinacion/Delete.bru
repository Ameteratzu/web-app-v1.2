meta {
  name: Delete
  type: http
  seq: 2
}

delete {
  url: {{Server}}/api/v1/direcciones-coordinaciones-emergencias/{{IdDireccionCoordinacionEmergencia}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 204
}
